# Atlas TDesign Vue Adapter

`@atlas-eids/adapter-tdesign-vue` 提供 Atlas Token 到 TDesign Vue Next 的运行时 Wrapper 与 Vue Plugin。

```ts
import { AtlasTDesign } from '@atlas-eids/adapter-tdesign-vue'

app.use(AtlasTDesign)
```

当前覆盖 Button、Input、Select、Table、Dialog、Drawer、Tabs 与 Form，状态为 Experimental。

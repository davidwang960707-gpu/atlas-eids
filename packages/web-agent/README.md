# @atlas-eids/web-agent

面向网页 Agent 的权限化工具注册、DOM 工具、高风险 Execution ID、审批、服务端审计回放、跨页面执行、`atlasAgent` API 和 `document.modelContext` WebMCP Bridge。

状态：Experimental。已提供当前 WebMCP Draft 的工具注册与契约检查，但浏览器实现仍在演进，生产使用需保留能力检测和服务端权限校验。

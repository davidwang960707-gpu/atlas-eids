# @atlas-eids/tokens

Atlas EIDS Design Tokens。`src/tokens.json` 是单一数据源，构建后输出 CSS、JSON 和 TypeScript。

```bash
npm run build -w @atlas-eids/tokens
```

```css
@import '@atlas-eids/tokens/tokens.css';
```

状态：Beta。Token 修改规范见 [`docs/DESIGN_TOKENS.md`](../../docs/DESIGN_TOKENS.md)。

# @atlas-eids/react

Atlas EIDS React 组件包，包含基础组件、Living Orb、AI Composer 和 Execution Plan。

```tsx
import { AtlasButton, AtlasOrb } from '@atlas-eids/react'
import '@atlas-eids/react/styles.css'
```

状态：Beta。

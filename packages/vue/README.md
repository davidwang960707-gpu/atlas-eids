# @atlas-eids/vue

Atlas EIDS Vue 3 组件包，与 React 包保持 66 个 UI 组件的相同语义、状态和视觉类名，并提供全局安装插件 `AtlasEIDS`。企业组合组件统一应用框架、页面标题和数据工作区；AI 组件覆盖会话、知识、Artifact、结构化输入、Provenance、GenUI、MCP、跨页面 Agent 和工具调用。

```ts
import { AtlasEIDS } from '@atlas-eids/vue'
import '@atlas-eids/vue/styles.css'
```

状态：Beta。

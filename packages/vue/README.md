# @atlas-eids/vue

Atlas EIDS Vue 3 组件包，与 React 包保持 50 个 UI 组件的相同语义、状态和视觉类名，并提供全局安装插件 `AtlasEIDS`。企业组合组件统一页面标题和数据工作区；AI 组件覆盖会话、知识、MCP、引用、检索轨迹和工具调用。

```ts
import { AtlasEIDS } from '@atlas-eids/vue'
import '@atlas-eids/vue/styles.css'
```

状态：Beta。

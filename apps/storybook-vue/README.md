# Atlas EIDS Vue Storybook

Vue 正式组件与 AI 组件的交互工作台。50 个 UI 组件均提供状态矩阵与 Axe 检查，并包含与 React 对齐的 AI 原生组件分组；Button、Living Orb 和 AI Composer 支持 Controls 实时调参。

```bash
npm run dev:storybook:vue
```

默认地址为 `http://localhost:6007`，静态构建使用 `npm run build -w atlas-eids-storybook-vue`。

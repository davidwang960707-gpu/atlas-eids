# Atlas EIDS React Storybook

React 正式组件、AI 组件与 Ant Design Runtime Wrapper 的交互工作台。31 个组件均提供状态矩阵与 Axe 检查，Button、Living Orb 和 AI Composer 支持 Controls 实时调参。

```bash
npm run dev:storybook:react
```

默认地址为 `http://localhost:6006`，静态构建使用 `npm run build -w atlas-eids-storybook-react`。
